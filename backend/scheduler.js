/**
 * DEPRECATED - Removed 2025
 * 
 * Scheduler logic moved to Next.js /api/fetch route.
 * Cron job pings port 3000 every 5 minutes to trigger fetches.
 */

module.exports = { start: () => {}, stop: () => {}, lastFetchTime: () => null };

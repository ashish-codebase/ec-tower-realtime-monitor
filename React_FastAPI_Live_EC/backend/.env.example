# Backend environment variables
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Turso (libsql) — free tier SQLite database
# Get these from https://turso.tech — create a database, then generate an auth token
TURSO_IP="libsql://your-database.aws-us-west-2.turso.io"
TURSO_KEY="your-auth-token-here"
